apiVersion: v1
kind: Pod
metadata:
  name: gpu-fault-cross-window-xid11-node00
  namespace: gpu-fault-system
  labels:
    gpu-fault.io/test-case: cross-fault-within-window
spec:
  nodeSelector:
    gpu-fault.io/e2e-target-a: "true"
  restartPolicy: Never
  containers:
    - name: injector
      resources:
        requests:
          cpu: "10m"
          memory: 32Mi
        limits:
          cpu: "200m"
          memory: 128Mi
      image: public.ecr.aws/docker/library/busybox:1.36
      command: ["/bin/sh", "-c"]
      args:
        - |
          set -eu
          sleep 10
          date -u +"xid11_written_at=%Y-%m-%dT%H:%M:%SZ"
          printf '%s\n' \
            'NVRM: Xid (PCI:0000:59:00): 11, pid=4242, name=python, Invalid or corrupted push buffer stream' \
            > /host-dev-kmsg
      securityContext:
        privileged: true
      volumeMounts:
        - name: dev-kmsg
          mountPath: /host-dev-kmsg
  volumes:
    - name: dev-kmsg
      hostPath:
        path: /dev/kmsg
        type: CharDevice
---
apiVersion: v1
kind: Pod
metadata:
  name: gpu-fault-cross-window-sxid10003-node05
  namespace: gpu-fault-system
  labels:
    gpu-fault.io/test-case: cross-fault-within-window
spec:
  nodeSelector:
    gpu-fault.io/e2e-target-b: "true"
  restartPolicy: Never
  containers:
    - name: injector
      resources:
        requests:
          cpu: "10m"
          memory: 32Mi
        limits:
          cpu: "200m"
          memory: 128Mi
      image: public.ecr.aws/docker/library/busybox:1.36
      command: ["/bin/sh", "-c"]
      args:
        - |
          set -eu
          sleep 11
          date -u +"sxid10003_written_at=%Y-%m-%dT%H:%M:%SZ"
          printf '%s\n' \
            'nvidia-nvswitch3: SXid (PCI:0000:c1:00.0): 10003, Fatal, Link 46 Host_unhandled_interrupt' \
            >> /host-fabricmanager-log
      securityContext:
        privileged: true
      volumeMounts:
        - name: fabricmanager-log
          mountPath: /host-fabricmanager-log
  volumes:
    - name: fabricmanager-log
      hostPath:
        path: /var/log/fabricmanager.log
        type: File
